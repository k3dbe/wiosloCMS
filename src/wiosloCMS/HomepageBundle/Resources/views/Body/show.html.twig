{% extends "::base.html.twig" %}

{% block body %}

    {% if id is defined and id %}
        {{ render(controller('PhotoBundle:Photo:show', {'id': id})) }}
    {% else %}
        {{ render(controller('PhotoBundle:Photo:show')) }}
    {% endif %}

{% endblock %}
